let filter = lam f, xs .
  match xs with
  | [] -> []
  | x :: xs' -> if f x
                then x :: (filter f xs')
                else filter f xs'
  end
in
  let is_even = lam x . if x % 2 == 0 then true else false in
    filter is_even [1, 2, 3, 4, 5, 6]
  